# Cross-agent code owners for all paths. Combined with branch protection
# "Require review from Code Owners," this enforces the COLLABORATION.md
# rule 2 cross-approval requirement: GitHub blocks self-approval, so every
# PR needs an approving review from the paired agent.
#
# The hack/merge-pr.sh wrapper additionally requires the approval be from
# the specific paired agent (j2d3 <-> 5u6r054) on the exact current head
# commit, not merely from any non-author code owner.
*                              @j2d3 @5u6r054
