apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: web3signer-validator-keystore
  namespace: signing
  labels:
    app.kubernetes.io/name: web3signer
    app.kubernetes.io/part-of: ethereum-validator-platform
spec:
  refreshInterval: 15m
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-signing-secrets
  target:
    name: web3signer-validator-keystore
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      type: Opaque
      data:
        validator.json: "{{ .keystore }}"
        validator.password: "{{ .password }}"
        validator.yaml: |
          type: "file-keystore"
          keyType: "BLS"
          keystoreFile: "/var/run/web3signer/keys/validator.json"
          keystorePasswordFile: "/var/run/web3signer/keys/validator.password"
        validator-02.json: "{{ .keystore02 }}"
        validator-02.password: "{{ .password02 }}"
        validator-02.yaml: |
          type: "file-keystore"
          keyType: "BLS"
          keystoreFile: "/var/run/web3signer/keys/validator-02.json"
          keystorePasswordFile: "/var/run/web3signer/keys/validator-02.password"
        validator-03.json: "{{ .keystore03 }}"
        validator-03.password: "{{ .password03 }}"
        validator-03.yaml: |
          type: "file-keystore"
          keyType: "BLS"
          keystoreFile: "/var/run/web3signer/keys/validator-03.json"
          keystorePasswordFile: "/var/run/web3signer/keys/validator-03.password"
  data:
    - secretKey: keystore
      remoteRef:
        key: eth-validator-platform-dev/signing/validator-keystore
        property: keystore
    - secretKey: password
      remoteRef:
        key: eth-validator-platform-dev/signing/validator-keystore
        property: password
    - secretKey: keystore02
      remoteRef:
        key: eth-validator-platform-dev/signing/validator-keystore-02
        property: keystore
    - secretKey: password02
      remoteRef:
        key: eth-validator-platform-dev/signing/validator-keystore-02
        property: password
    - secretKey: keystore03
      remoteRef:
        key: eth-validator-platform-dev/signing/validator-keystore-03
        property: keystore
    - secretKey: password03
      remoteRef:
        key: eth-validator-platform-dev/signing/validator-keystore-03
        property: password
