apiVersion: v1
kind: ConfigMap
metadata:
  name: ethereum-node-sync-profile
  namespace: ethereum
  labels:
    app.kubernetes.io/part-of: ethereum-validator-platform
    platform.galaxy-lab/environment: dev
    platform.galaxy-lab/profile: ephemery-signing-recovery
    platform.galaxy-lab/signing-enabled: "true"
data:
  environment: dev
  profile: ephemery-signing-recovery
  network: ephemery-162
  signingEnabled: "true"
  explanation: "One deposited Ephemery assignment is restored for signing qualification; all other assignments remain stopped."
