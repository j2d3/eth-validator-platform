apiVersion: v1
kind: Service
metadata:
  name: portal-status-api
  namespace: portal-system
  labels:
    app.kubernetes.io/name: portal-status-api
    app.kubernetes.io/component: read-model
    app.kubernetes.io/part-of: ethereum-validator-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: portal-status-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
